# Production site URL — used for canonical links, Open Graph/Twitter metadata,
# the sitemap, and JSON-LD structured data. Falls back to a placeholder if unset.
NEXT_PUBLIC_SITE_URL=https://abhijithmd.com
